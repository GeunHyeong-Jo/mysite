<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">

	<select id="selectcount" resultType="int">
		<![CDATA[ 
			select count(*) from board
		]]>
	</select>

	<update id="updateorder" parameterType="hashmap">
		<![CDATA[
			update board SET o_no=o_no+1 WHERE g_no = #{g_no } AND o_no >= #{o_no }
		]]>	
	</update>
	
	<select id="findall" parameterType="hashmap" resultType="boardvo">
		<![CDATA[ 
			select no,title, author,views, reg_date, content, g_no,o_no,depth, user_no from board
			 order by g_no DESC, o_no ASC limit #{start}, #{end}
		]]>
	</select>
</mapper>
